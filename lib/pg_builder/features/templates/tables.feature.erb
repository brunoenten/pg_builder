Feature: Table <%= schema_name %>.<%= element_name %>
  Scenario: Table should exists
    Given the sql path '<%= task.name %>'
    Then the table '<%= schema_name %>.<%= element_name %>' should exists

